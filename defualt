server {
        listen   80;
        index index.html index.htm;
        root /var/www;
        server_name localhost;

        location / {
                root /var/www;
                try_files $uri $uri/ /index.html =404;
                autoindex off;
        }
}
